#name: Publish

#on:
  #workflow_run:
  #  workflows: [ "Build" ]
  #  branches: [ "main" ]
  #  types:
  #    - completed
  #workflow_dispatch:

#jobs:
#  publish:
#    if: ${{ github.event.workflow_run.conclusion == 'success' }}
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - name: Setup JDK
#        uses: actions/setup-java@v4
#        with:
#          java-version: 8
#          distribution: 'temurin'
#      - name: Setup Gradle
#        uses: gradle/actions/setup-gradle@v4
#        with:
#          gradle-version: wrapper
#          cache-read-only: true
#      - name: Create release
#        uses:
#      - name: Publish
#        run: gradle publish
#        env:
#          PACKAGES_REPOSITORY_USERNAME: ${{ secrets.PACKAGES_REPOSITORY_USERNAME }}
#          PACKAGES_REPOSITORY_TOKEN: ${{ secrets.PACKAGES_REPOSITORY_TOKEN }}
